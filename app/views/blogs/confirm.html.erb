<h3>以下の内容でブログを作成する</h3>
<%= form_with(model: @blog, local: true) do |f| %>
  <%= f.hidden_field :title %>
  <%= f.hidden_field :content %>
  <p>タイトル<%= @blog.title %></p>
  <p>本文<%= @blog.content %></p>
  <%= f.submit '作成する' %>
  <%= f.submit '戻る', name: 'back' %>
<% end %>
